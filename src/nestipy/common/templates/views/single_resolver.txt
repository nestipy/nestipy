import strawberry
from nestipy.graphql import Resolver, Query, Mutation


@Resolver()
class {{name|capitalize}}Resolver:

    @Query()
    def {{name|lower}}_test_query(self) -> str:
        return "test"

    @Mutation()
    def {{name|lower}}_test_mutation(self,test: str) -> str:
        return test
